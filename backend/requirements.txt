# NeuroDrug API - Inference-only dependencies (Render deploy)
# Trimmed from the full research requirements.txt: this service runs
# FastAPI + inference only (no celery worker, no mlflow, no offline
# training/reporting libs). Those live in the local Docker Compose
# stack, not here.

# ─── Web Framework ─────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.0
orjson==3.10.3
python-multipart==0.0.9
httpx==0.27.0
aiohttp==3.9.5

# ─── Database ──────────────────────────────────────────────
sqlalchemy[asyncio]==2.0.30
alembic==1.13.1
asyncpg==0.29.0
psycopg2-binary==2.9.9

# ─── Cache / Task Queue ─────────────────────────────────────
# celery is imported by app/tasks/etl_tasks.py (via etl endpoint router)
# even though no worker runs on this service - kept to avoid ImportError.
redis[hiredis]==5.0.4
celery==5.4.0

# ─── Auth ───────────────────────────────────────────────────
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==3.2.2
email-validator==2.1.1

# ─── Validation ─────────────────────────────────────────────
pydantic==2.7.2
pydantic-settings==2.2.1

# ─── ML / Graph (CPU-only torch - no CUDA needed on Render) ─
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.3.0+cpu
torch-geometric==2.5.3
scikit-learn==1.5.0
numpy==1.26.4
networkx==3.3

# ─── Observability ──────────────────────────────────────────
prometheus-client==0.20.0
sentry-sdk[fastapi]==2.3.1
structlog==24.2.0
python-json-logger==2.0.7

# ─── Retry / Resilience ─────────────────────────────────────
tenacity==8.3.0
